# 变更日志

格式遵循语义化版本；与 `pyproject.toml` / `ops_distiller_forge.__version__` 对齐。

## [0.1.0] - 2026-04-17

### 新增

- 包 **ops-distiller-forge**：Ontology（`KnowledgePoint`、`AgentManifestV1`）、JSONL/SQLite、`map` / `episodes` / `reduce` / `export-manifest` / `eval-recall` CLI。
- 可选 **`[dspy]`**：`dspy-ai` + DSPy `ChainOfThought` 蒸馏（`map --use-dspy`）。
- 文档：`ENGINEERING.md`、`CONTRACT.md`、根 `README.md`。
